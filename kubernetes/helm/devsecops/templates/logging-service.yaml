apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.logging.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.services.logging.name }}
spec:
  replicas: {{ .Values.services.logging.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.services.logging.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.logging.name }}
    spec:
      serviceAccountName: {{ .Values.services.logging.name }}
      automountServiceAccountToken: false
      containers:
        - name: {{ .Values.services.logging.name }}
          image: "{{ .Values.services.logging.image }}:{{ .Values.services.logging.tag }}"
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.services.logging.port }}
              protocol: TCP

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.logging.port }}
            initialDelaySeconds: 5
            periodSeconds: 10

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.logging.port }}
            initialDelaySeconds: 10
            periodSeconds: 20

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL

---

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.logging.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.services.logging.name }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.services.logging.port }}
      targetPort: {{ .Values.services.logging.port }}
  type: ClusterIP
