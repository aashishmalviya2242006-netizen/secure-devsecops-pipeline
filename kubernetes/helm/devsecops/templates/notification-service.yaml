apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.services.notification.name }}
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Values.services.notification.name }}
spec:
  replicas: {{ .Values.services.notification.replicas }}
  selector:
    matchLabels:
      app: {{ .Values.services.notification.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.services.notification.name }}
    spec:
      serviceAccountName: {{ .Values.services.notification.name }}
      automountServiceAccountToken: false
      containers:
        - name: {{ .Values.services.notification.name }}
          image: "{{ .Values.services.notification.image }}:{{ .Values.services.notification.tag }}"
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.services.notification.port }}
              protocol: TCP

          readinessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.notification.port }}
            initialDelaySeconds: 5
            periodSeconds: 10

          livenessProbe:
            httpGet:
              path: /health
              port: {{ .Values.services.notification.port }}
            initialDelaySeconds: 10
            periodSeconds: 20

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL

---

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.notification.name }}
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: {{ .Values.services.notification.name }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.services.notification.port }}
      targetPort: {{ .Values.services.notification.port }}
  type: ClusterIP
