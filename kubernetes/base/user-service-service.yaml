apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: devsecops
spec:
  selector:
    app: user-service
  ports:
    - name: http
      protocol: TCP
      port: 8001
      targetPort: 8001
  type: ClusterIP
