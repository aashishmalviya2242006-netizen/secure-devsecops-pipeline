apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: devsecops

spec:
  selector:
    app: notification-service

  ports:
    - name: http
      protocol: TCP
      port: 8003
      targetPort: 8003

  type: ClusterIP
