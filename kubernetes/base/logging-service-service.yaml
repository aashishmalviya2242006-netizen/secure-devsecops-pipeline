apiVersion: v1
kind: Service

metadata:
  name: logging-service
  namespace: devsecops

spec:
  selector:
    app: logging-service

  ports:
    - name: http
      protocol: TCP
      port: 8004
      targetPort: 8004

  type: ClusterIP
